# TODO: write a function `sort`, to sort user's prompt with gemini-2.5-flash without thinking. The sorting categories are: "question", "action"
from google.genai import types
import json
from utils import client, add_generative_cost

def sort(prompt, silent=False):
    pass
